---
description: "Force tracked item to Done"
allowed-tools: ["Bash(node:*)"]
---

Run this command and report the output to the user:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/tracker.mjs" cmd finish "${CLAUDE_SESSION_ID}"
```
