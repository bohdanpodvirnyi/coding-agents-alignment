---
description: "Force aligned item to Done"
allowed-tools: ["Bash(node:*)"]
---

Run this command and report the output to the user:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/alignment.mjs" cmd finish "${CLAUDE_SESSION_ID}"
```
