---
description: "Re-sync aligned item with GitHub"
allowed-tools: ["Bash(node:*)"]
---

Run this command and report the output to the user:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/alignment.mjs" cmd resync "${CLAUDE_SESSION_ID}"
```
