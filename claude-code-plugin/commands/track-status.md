---
description: "Show current GitHub Project tracking state"
allowed-tools: ["Bash(node:*)"]
---

Run this command and report the output to the user:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/tracker.mjs" cmd status "${CLAUDE_SESSION_ID}"
```
